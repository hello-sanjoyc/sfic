"use client";
import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";
import { z } from "zod";
const schema = z.object({ name: z.string().min(2, "Enter your name"), email: z.string().email("Enter a valid email"), mobile: z.string().min(10, "Enter a valid mobile number"), category: z.string().min(1, "Choose a category"), subject: z.string().min(3, "Enter a subject"), message: z.string().min(10, "Please add more detail") });
type Values = z.infer<typeof schema>;
export function ContactForm() { const { register, handleSubmit, formState: { errors, isSubmitSuccessful } } = useForm<Values>({ resolver: zodResolver(schema) }); const field = "mt-1 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm"; return <form noValidate onSubmit={handleSubmit(() => undefined)} className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-bold text-[#0b1f3a]">Send an enquiry</h2>{isSubmitSuccessful && <p role="status" className="rounded-xl bg-green-50 p-3 text-sm text-green-800">Thank you. Your enquiry has been recorded.</p>}<label className="text-sm font-medium">Name<input className={field} {...register("name")} />{errors.name && <span className="text-xs text-red-700">{errors.name.message}</span>}</label><label className="text-sm font-medium">Email<input className={field} type="email" {...register("email")} />{errors.email && <span className="text-xs text-red-700">{errors.email.message}</span>}</label><label className="text-sm font-medium">Mobile<input className={field} {...register("mobile")} />{errors.mobile && <span className="text-xs text-red-700">{errors.mobile.message}</span>}</label><label className="text-sm font-medium">Category<select className={field} {...register("category")}><option value="">Select a category</option><option>Registration</option><option>Eligibility</option><option>Technical support</option></select>{errors.category && <span className="text-xs text-red-700">{errors.category.message}</span>}</label><label className="text-sm font-medium">Subject<input className={field} {...register("subject")} />{errors.subject && <span className="text-xs text-red-700">{errors.subject.message}</span>}</label><label className="text-sm font-medium">Message<textarea className={field} rows={4} {...register("message")} />{errors.message && <span className="text-xs text-red-700">{errors.message.message}</span>}</label><button className="rounded-full bg-[#0b1f3a] px-4 py-3 font-bold text-white hover:bg-[#000080]">Submit enquiry</button></form>; }
